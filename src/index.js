import './styles.css';
import * as ui from './ui';
import * as logic from './logic';

// The UI and logic modules handle their own initial setup and event listeners.
// You might add any global event listeners or initializations here if needed.

// Example of initial load (already handled within logic and UI):
document.addEventListener('DOMContentLoaded', () => {
    // ui.renderProjects(); // Already called in ui.js
    // ui.renderTodos();    // Already called in ui.js
});